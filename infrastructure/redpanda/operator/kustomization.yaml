# infrastructure/redpanda/operator/kustomization.yaml
#
# Kustomize manifest for the operator layer. Flux's kustomize-controller runs
# `kustomize build` here before applying.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
