apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: podinfo
  namespace: podinfo
spec:
  parentRefs:
  - name: homelab-gateway
    namespace: podinfo
  hostnames:
  - podinfo.homelab.local
  rules:
  - backendRefs:
    - name: podinfo
      port: 9898
